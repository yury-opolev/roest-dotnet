# .NET
bin/
obj/
*.user
*.suo
.vs/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build outputs
*.wav
!server/voices/*.wav

# HuggingFace cache (also volume-mounted)
hf_cache/

# Large generated ONNX runtime artifacts — distributed via GitHub Releases,
# regenerated by native-onnx/export/*.py. Too big for git.
native-onnx/onnx_models/
native-onnx/refs/*.bin
native-onnx/refs/*.npz

# Third-party chatterbox source copies kept locally only for reference (MIT,
# re-fetchable). Not redistributed here.
native-onnx/export/_ref_*.py

# Generated audio
samples/
samples_native/

# Release build artifacts (uploaded to GitHub Releases, not committed)
release-artifacts/
*.nupkg
danish-voice-native-runtime.zip

# IDE
.idea/
.vscode/
